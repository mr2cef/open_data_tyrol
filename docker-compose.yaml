version: "3.9"
services:
  collect:
    build: .
    ports:
      - "8080:8080"
  influxdb:
    image: "influxdb:2.0"
    ports:
      - "8086:8086"
    volumes: 
      - type: bind
        source: "/home/martin/influxdb/"
        target: "/var/lib/influxdb2"
